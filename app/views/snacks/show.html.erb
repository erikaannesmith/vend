<%= @snack.name %>
Price: $<%= @snack.price %>

Locations:

<% @snack.machines.each do |machine| %>
  <%= machine.location %> (<%= machine.snacks.count %> kind of snack, average price of $<%= machine.average_price %>)
<% end %>
